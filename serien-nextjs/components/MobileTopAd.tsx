'use client';

/**
 * MobileTopAd — DEAKTIVIERT (Feb 2026, User-Vorgabe).
 * Auf Mobile werden GAR KEINE Ads mehr ausgeliefert. Komponente rendert
 * hart `null`. Die Datei bleibt bestehen, damit LayoutWrapper-Imports
 * nicht brechen — falls Mobile-Ads irgendwann wieder aktiviert werden,
 * kann die alte Custom-HTML-Logik aus dem Git-History wiederhergestellt
 * werden.
 */
export default function MobileTopAd() {
  return null;
}
